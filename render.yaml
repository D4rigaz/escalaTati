services:
  - type: web
    name: escalatati
    env: node
    plan: free
    buildCommand: npm ci && npm run build --workspace=frontend
    startCommand: npm start --workspace=backend
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        sync: false
